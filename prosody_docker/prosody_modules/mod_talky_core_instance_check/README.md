# Talky Core Instance Check

This module allows the service to determine if a domain is pointing to itself.

## Configuration

- `talky_core_instance_check_url` - API URL used to retrieve instance identifier.
